<!--コメント作成機能-->


        <%= form_with(model:[book, @book_comment], remote: true) do |f| %>
        <!--Ajax処理-->
          <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに" %>
          <%= f.submit "送信する" %>
        <% end %>