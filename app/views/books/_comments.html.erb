<!--削除ボタン-->

          <% if book_comment.user == current_user %>
          <p><%= link_to 'Delete', book_book_comment_path(book_comment.book, book_comment),class: "btn btn-danger", method: :delete, remote: true  %></p>
          <!--引数を二つ使う。rails routesで確認-->
          <!--Ajax処理実装-->
         <% end %>
         